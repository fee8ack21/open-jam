# 專案
PROJECT_NAME=open-jam

# 部署路徑
EXTERNAL_URL=http://localhost/

# 資料庫
DB_HOST=${PROJECT_NAME}-postgres
DB_NAME_PREFIX=open_jam
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=

# Redis
REDIS_PASSWORD=

# RabbitMQ
RABBITMQ_HOST=rabbitmq
RABBITMQ_VIRTUAL_HOST=${PROJECT_NAME}
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=

# email smtp（本地開發預設 mailpit：SMTP_HOST=mailpit, PORT=1025, SSL=false）
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_ENABLE_SSL=false
SMTP_FROM=noreply@openjam.co

# 身份認證
HYDRA_PUBLIC_URL=http://localhost:4444/
AUTHORITY=${HYDRA_PUBLIC_URL}
AUTH_PAGE_URL=${EXTERNAL_URL}auth/
PORTAL_PAGE_URL=${EXTERNAL_URL}
WORKSPACE_URL=${EXTERNAL_URL}workspace/
CREATOR_BASE_URL=${EXTERNAL_URL}creator/
STORE_API_URL=http://localhost:5172
CATALOG_API_URL=http://localhost:5176
LOG_API_URL=http://localhost:5170
CONTENT_API_URL=http://localhost:5181
STORE_SLUG=xiaoming-shop
GITHUB_REPO_URL=https://github.com/fee8ack21/open-jam
DOCS_URL=https://docs.openjam.co
WEB_CLIENT_ID=open-jam-web
SERVICE_AUTHORITY=${HYDRA_PUBLIC_URL}
SERVICE_CLIENT_ID=open-jam-service
SERVICE_CLIENT_SECRET=

# Hydra
HYDRA_PUBLIC_PORT=4444
HYDRA_ADMIN_PORT=4445
HYDRA_ADMIN_URL=http://${PROJECT_NAME}-hydra:4445
HYDRA_INTERNAL_URL=http://${PROJECT_NAME}-hydra:4444/.well-known/openid-configuration
HYDRA_DSN=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME_PREFIX}_hydra
HYDRA_URLS_SELF_ISSUER=${HYDRA_PUBLIC_URL}
HYDRA_URLS_LOGIN=${AUTH_PAGE_URL}login
HYDRA_URLS_CONSENT=${AUTH_PAGE_URL}consent
HYDRA_URLS_LOGOUT=${AUTH_PAGE_URL}logout
HYDRA_URLS_ERROR=${AUTH_PAGE_URL}error
HYDRA_SERVE_PUBLIC_CORS_ALLOWED_ORIGINS=*
HYDRA_SECRETS_SYSTEM=
HYDRA_OIDC_SUBJECT_IDENTIFIERS_SUPPORTED_TYPES=pairwise,public
HYDRA_OIDC_SUBJECT_IDENTIFIERS_PAIRWISE_SALT=
HYDRA_TTL_AUTH_CODE=10m
HYDRA_TTL_LOGIN_CONSENT_REQUEST=10m
HYDRA_LOG_LEAK_SENSITIVE_VALUES=false

AUTH__INTERNAL_URL=http://${PROJECT_NAME}-auth:8080/
AUTH__EXTERNAL_URL=${EXTERNAL_URL}auth/
AUTH__CONNECTION_STRING=Host=${DB_HOST}:${DB_PORT};Database=${DB_NAME_PREFIX}_auth;Username=${DB_USER};Password=${DB_PASSWORD}

# Stripe（test mode，正式以 Secret Manager 覆蓋）
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
PAYMENT_SUCCESS_URL=${EXTERNAL_URL}checkout/success?session_id={CHECKOUT_SESSION_ID}
PAYMENT_CANCEL_URL=${EXTERNAL_URL}checkout/cancel

# 通知信商品連結樣板（NotificationService，佔位符 {storeSlug} / {catalogId}）
NOTIFICATION_CATALOG_URL_PATTERN=https://{storeSlug}.openjam.co/product/{catalogId}

# 檔案儲存（StorageService；地端用本地檔案系統，正式環境改 Provider=Gcs）
STORAGE_PROVIDER=Local
STORAGE_BUCKET=open-jam
STORAGE_PUBLIC_BASE_URL=http://localhost:5171/v1/files/blob
STORAGE_LOCAL_ROOT_PATH=Files
STORAGE_LOCAL_BASE_URL=http://localhost:5171
STORAGE_LOCAL_SIGNING_KEY=
